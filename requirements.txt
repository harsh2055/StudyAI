flask==3.0.3
openai==1.35.3
pypdf==4.2.0
werkzeug==3.0.3
python-dotenv==1.0.1
# No extra libraries needed for Phase 2!
# SQLite is built into Python standard library.
# OpenAI SDK already covers Whisper (speech-to-text) and TTS (text-to-speech).
